package
{{package}}

import ( "net/http" "net/http/httptest" "testing" ) func Test{{Name}}Handler_Handle(t *testing.T) { handler := New{{Name}}Handler()
tests := []struct { name string method string path string wantStatus int }{ { name: "successful
{{description}}", method: http.MethodGet, path: "/{{path}}", wantStatus: http.StatusOK, }, // TODO: add more test cases
} for _, tt := range tests { t.Run(tt.name, func(t *testing.T) { req := httptest.NewRequest(tt.method, tt.path, nil) w
:= httptest.NewRecorder() handler.Handle(w, req) if w.Code != tt.wantStatus { t.Errorf("got status %d, want %d", w.Code,
tt.wantStatus) } }) } }